
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>Dali WebAR Test — Elephant, Cloud, Man</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <!-- A-Frame (WebXR/3D framework) -->
  <script src="https://cdn.jsdelivr.net/npm/aframe@1.5.0/dist/aframe.min.js"></script>
  <!-- MindAR (Image tracking + A-Frame integration) -->
  <script src="https://cdn.jsdelivr.net/npm/mind-ar@1.1.6/dist/mindar-image-aframe.prod.js"></script>

  <style>
    /* Fullscreen, no margins */
    html, body { margin:0; padding:0; height:100%; font-family: system-ui, Arial; }

    /* Top caption for simple guidance */
    .caption {
      position: fixed; top: 10px; left: 10px; right: 10px;
      background: rgba(0,0,0,0.5); color:#fff; padding:8px 12px;
      border-radius:6px; font-size:14px; z-index:10;
    }

    /* Bottom UI bar for quick help */
    .ui-bar {
      position: fixed; left: 0; right: 0; bottom: 0;
      background: rgba(0,0,0,0.6); color: #fff; padding: 10px;
      display: flex; gap: 8px; justify-content: center; align-items: center; z-index: 10;
    }
    .ui-btn {
      background: #ffd700; color: #000; border: none; padding: 8px 12px;
      font-weight: 600; border-radius: 6px; cursor: pointer;
    }
  </style>
</head>

<body>
  <!-- Simple caption (you can tweak text later) -->
  <div class="caption">
    Point your phone at the Dali image. The elephant walks, the cloud floats, the man crosses. Screenshot to capture!
  </div>

  <!-- WebAR scene using your compiled Dali target -->
  <a-scene
    mindar-image="imageTargetSrc: ./assets/dali.mind"
    vr-mode-ui="enabled: false"
    device-orientation-permission-ui="enabled: true"
    renderer="colorManagement: true, physicallyCorrectLights: true"
    embedded
  >
    <!-- Preload sprites -->
    <a-assets>
      <img id="elephant-img" src="./assets/elephant.png" crossorigin="anonymous" />
      <img id="cloud-img" src="./assets/cloud.png" crossorigin="anonymous" />
      <img id="man-img" src="./assets/man.png" crossorigin="anonymous" />
    </a-assets>

    <!-- Camera (phone camera feed appears behind this scene) -->
    <a-camera position="0 0 0" look-controls="enabled: false"></a-camera>

    <!-- All content inside this entity appears when the Dali image is recognised -->
    <a-entity mindar-image-target="targetIndex: 0">

      <!-- 1) Elephant: gentle walk out (left → right). Adjust position/scale to align with your image -->
      <a-image
        src="#elephant-img"
        position="-0.25 -0.28 0"     <!-- x left/right, y up/down, z depth -->
        scale="0.55 0.55 1"
        material="transparent: true; alphaTest: 0.5"
        animation__move="property: position; to: 0.25 -0.28 0; dur: 4200; dir: alternate; loop: true; easing: easeInOutQuad"
      ></a-image>

      <!-- 2) Cloud: slow float upward, then down -->
      <a-image
        src="#cloud-img"
        position="0.05 0.30 0"
        scale="0.35 0.35 1"
        material="transparent: true; alphaTest: 0.5"
        animation__float="property: position; to: 0.05 0.52 0; dur: 5000; dir: alternate; loop: true; easing: easeInOutSine"
      ></a-image>

      <!-- 3) Man with pipe: cross the scene (right → left) -->
      <a-image
        src="#man-img"
        position="0.35 -0.10 0"
        scale="0.40 0.40 1"
        material="transparent: true; alphaTest: 0.5"
        animation__walk="property: position; to: -0.35 -0.10 0; dur: 4500; dir: alternate; loop: true; easing: easeInOutQuad"
      ></a-image>

      <!-- Optional accent ring (remove or reposition). Use this to mark a point of interest -->
      <a-ring
        position="0.00 -0.18 0"
        radius-inner="0.08"
        radius-outer="0.10"
        color="#ffd700"
        opacity="0.85"
        animation__pulse="property: scale; to: 1.2 1.2 1; dir: alternate; loop: true; dur: 1200"
      ></a-ring>

    </a-entity>
  </a-scene>

  <!-- Bottom UI: quick help + screenshot tip -->
  <div class="ui-bar">
    <button class="ui-btn" id="btn-help">Screenshot Tips</button>
  </div>

  <script>
    // Help button for quick capture instructions
    document.getElementById('btn-help').addEventListener('click', () => {
      alert(
        'Screenshot tips:\\n\\n' +
        '- iPhone: Side Button + Volume Up.\\n' +
        '- Android: Power + Volume Down.\\n' +
        'Pose while the AR sprites move!'
      );
    });
  </script>
</body>
</html>
